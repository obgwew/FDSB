<dash>
name: $kick
desc: یک کاربر را از سرور اخراج می‌کند.
category: مدیریت

<block type="text" title="این دستور چگونه کار می‌کند؟">
text: وقتی $kick بدون آرگومان فراخوانی شود، نویسنده دستور را اخراج می‌کند. وقتی با شناسه کاربر یا منشن فراخوانی شود، آن کاربر خاص را اخراج می‌کند. بات شناسه عددی کاربر را از آرگومان ارائه شده استخراج کرده و از طریق Discord API اخراج را انجام می‌دهد.
</block>

<block type="code" title="سینتکس">
text: $kick
</block>

<block type="code" title="مثال آماده کپی">
text: $kick[123456789]
</block>

<block type="callout" kind="warning">
text: بات باید دسترسی "Kick Members" را داشته باشد و بالاترین نقش آن باید بالاتر از بالاترین نقش هدف باشد. استفاده از $kick بدون براکت کاربری که دستور را اجرا کرده است را اخراج می‌کند.
</block>
</dash>
