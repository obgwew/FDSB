<dash>
name: $channelID
desc: شناسه کانال دیسکوردی را که دستور در آن استفاده شده برمی‌گرداند.
category: اطلاعات

<block type="text" title="این دستور چگونه کار می‌کند؟">
text: وقتی $channelID فراخوانی می‌شود، ربات شناسه کانال یکتای دیسکورد کانالی را که دستور در آن اجرا شده دریافت می‌کند و آن را مستقیماً به‌عنوان یک رشته عددی خام می‌فرستد.
</block>

<block type="code" title="سینتکس">
text: $channelID
</block>

<block type="code" title="مثال آماده کپی">
text: This command was used in channel $channelID
</block>

<block type="callout" kind="note">
text: مقدار بازگشتی یک شناسه snowflake است (مثلاً 123456789012345678)، که به‌طور یکتا کانالی را که پیام در آن ارسال شده شناسایی می‌کند.
</block>
</dash>