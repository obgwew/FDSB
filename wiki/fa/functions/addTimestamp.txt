<dash>
name: $addTimestamp
desc: زمان فعلی را دریافت می‌کند و آن را به‌صورت یک قالب زمان پویای دیسکورد می‌فرستد.
category: زمان‌بندی و تاخیر

<block type="text" title="این دستور چگونه کار می‌کند؟">
text: وقتی $addTimestamp فراخوانی می‌شود، ربات زمان فعلی را محاسبه می‌کند، آن را به قالب زمان پویای دیسکورد (قالب زمان طولانی) تبدیل می‌کند و نتیجه را مستقیماً به کانال مقصد می‌فرستد.
</block>

<block type="code" title="سینتکس">
text: $addTimestamp
</block>

<block type="code" title="مثال آماده کپی">
text: $addTimestamp
</block>

<block type="callout" kind="note">
text: زمان ارسال‌شده از قالب زمان طولانی استفاده می‌کند، به این معنی که به‌طور خودکار مطابق منطقه زمانی محلی دستگاه هر کاربر دیسکورد تنظیم و نمایش داده می‌شود (مثلاً 6:46:58 PM).
</block>
</dash>
